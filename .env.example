# Browser requests stay on the Next.js origin so Django session cookies work.
NEXT_PUBLIC_API_BASE_URL=/backend

# Server-side target used by the /backend/* Next.js rewrite.
# BACKEND_API_ORIGIN=http://localhost:8000
BACKEND_API_ORIGIN=http://172.168.1.245:8000
